def sub_pog(string)
	s = string.split("")
	h = Hash.new
	s.map! { |x| Base64.encode64(x)}
	counter = 0.b(256)
	s.each { |x|
		if h.key?(x)
			next
		else
			h.merge!({x => counter})
			counter += 1
		end
	}
	return h

end
